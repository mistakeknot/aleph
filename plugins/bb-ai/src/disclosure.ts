export const BB_CLOUD_DISCLOSURE =
  "With bb cloud on, bb sends the text of a thread's first prompt (for its title) and the changed files with a diff excerpt (for a commit message) to getbb.app, which forwards them to OpenRouter model providers with zero data retention. bb stores your daily usage totals and, for 30 days, metadata about each request such as its time, model, token counts, and cost. It never stores prompts or replies.";

export const BB_CLOUD_OFF_MESSAGE =
  "bb cloud is off. Turn it on in Settings → bb cloud AI or with `bb ai on`.";
